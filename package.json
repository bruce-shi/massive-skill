{
  "name": "massive-cli",
  "version": "0.0.1",
  "description": "Polygon/Massive Market Data CLI",
  "module": "index.ts",
  "type": "module",
  "bin": {
    "massive": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/cli.ts --outdir dist --target node",
    "start": "bun dist/cli.js",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "@massive.com/client-js": "^10.3.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}